apiVersion: v1
kind: Service
metadata:
  name: "wfdfrontend-service"
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the frontend service.
  #type: LoadBalancer
  type: NodePort
  ports:
  # the port that this service should serve on
  - name: http
    port: 9080
  - name: https
    port: 9443

  selector:
    app: "wfdfrontend-selector"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "wfdfrontend-deployment"
spec:
  # this replicas value is default
  # modify it according to your case
  replicas: 1
  template:
    metadata:
      labels:
        app: "wfdfrontend-selector"
    spec:
      containers:
      - name: wfdfrontend
        image: wfdfrontend:latest
        readinessProbe:
          httpGet:
            path: /WfdFrontEnd/health
            port: 9080
          initialDelaySeconds: 20
        # resources:
        #   requests:
        #     cpu: 100m
        #     memory: 100Mi
